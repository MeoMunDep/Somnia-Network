version: '3.8'

services:
  somnia-bot:
    build:
      context: .
    container_name: somnia-bot
    restart: unless-stopped
    volumes:
      - .:/app
    environment:
      - NODE_ENV=production
    stdin_open: true
    tty: true
